local CarScene = {}

function CarScene:OnEnter(id)
    --加载场景预制体
    --控制相机

    --初始化所有车辆(模型,ui,绑定事件)
end

function CarScene:OnExit()
    --关闭预制体
end

function CarScene:InitCar(carId)
    --获取数据
    --获取模型
    
    --绑定模型的点击事件
    --绑定对话框的点击事件(两个的handler相同)
end